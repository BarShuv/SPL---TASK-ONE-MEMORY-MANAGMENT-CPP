#include "../include/Volunteer.h" 
#include "../include/Order.h" 
#include <string>
#include <vector>
using std::string;
using std::vector;

// CollectorVolunteer::CollectorVolunteer(int idP, string nameP, int coolDownP) : id(idP), name(nameP), coolDown(coolDownP),
//  completedOrderId(0), activeOrderId(0) {}
